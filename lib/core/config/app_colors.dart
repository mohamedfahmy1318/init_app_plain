import 'package:flutter/material.dart';

class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  // ==================== Primary Colors ====================
  static const Color primary = Color(0xFFFAD52E);
  static const Color primaryLight = Color(0xFFFDEB71);
  static const Color primaryDark = Color(0xFFC7A700);
  static const Color indicatorColor = Color(0xFFE6E6E6);
  // ==================== Semantic Colors ====================
  // Success (Green)
  static const Color success = Color(0xFF22C55E);
  static const Color successLight = Color(0xFFE9FAEF);
  // Error (Red)
  static const Color error = Color(0xffFF4C5E);
  // Warning (Yellow)
  static const Color warning = Color(0xffFFD600);

  // ==================== Background Colors ====================
  static const Color background = Color(0xFFFAFFFF);

  // ==================== Text Colors ====================
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF8B8B8B);
  static const Color textThird = Color(0xFF2C3E50);
  static const Color whiteText = Color(0xFFFFFFFF);
  static const Color textFourth = Color(0xFF2F3131);

  // ==================== Border & Divider Colors ====================
  static const Color borderColor = Color(0xFFE7E7E7);
  static const Color divider = Color(0xFFE5E7EB);
}
